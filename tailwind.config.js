module.exports = {
    theme: {
        extend: {
            opacity: {
                mask: "0.65",
            },
        },
    },
    // explicitly configuring PurgeCSS in postcss.config.js
    purge: false,
}
